import { makeStyles } from "@material-ui/core";

export const useStyleBodyNewsWeather = makeStyles((theme) => ({
  container: {
    padding: "15px",
    width: "100%",
    display: "flex",
    justifyContent: "space-between",
  },
  
}));
