import { makeStyles } from "@material-ui/core";

export const useStyleButtomWeatherMas = makeStyles((theme) => ({
  container: {
    padding: "0 2.2rem",
    width: "100%",
    position: "relative",
  },
  
}));